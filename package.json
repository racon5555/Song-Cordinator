{
  "name": "song-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Async AWS worker for the IBB DevOps Praxisaufgabe (coordinator webhook -> SQS -> worker -> result submit)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "synth": "cdk synth --strict",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "cdk": "cdk"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.1080.0",
    "@aws-sdk/client-sqs": "^3.700.0",
    "@aws-sdk/client-ssm": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.0.0",
    "aws-cdk": "^2",
    "aws-cdk-lib": "^2",
    "constructs": "^10.4.2",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
